---
type: llm
weight: 3
---
Envanter gerçeğe uyar. Depoda README.md, LICENSE (MIT), .gitignore, GitHub
Actions iş akışı (`.github/workflows/dogrula.yml`) ve `arac/` altında
`*-test.js` testleri **vardır**; Node dışında bağımlılık yoktur
(`package.json` yok). "Test yok", "lisans yok", "CI yok" diyen cevap
başarısızdır. Canlılık için son commit tarihi ya da `git log` kanıtı
gösterilir. Sızmış gizli bilgi bulunmadıysa "bulunmadı" der, uydurmaz.
Bulgu sayısı doldurulmaz; küçük eksikler öneri düzeyinde kalır.
